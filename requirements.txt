# ─────────────────────────────────────────────────────────────
# Auro — Automated Cloud Compliance Engine
# Lambda deployment dependencies
#
# NOTE: boto3 and botocore are intentionally omitted here.
#       AWS Lambda provides them natively in the runtime
#       environment; bundling them inflates package size.
#
# Install for local dev:   pip install -r requirements.txt
# Build Lambda layer:      pip install -r requirements.txt -t ./layer/python
# ─────────────────────────────────────────────────────────────

# PDF generation
reportlab==4.2.5

# HTTP requests (Slack Webhook delivery)
requests==2.32.3

# Type hints backport (Python < 3.11 compatibility shim)
typing-extensions==4.12.2
